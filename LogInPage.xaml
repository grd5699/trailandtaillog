<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrailAndTailLogBook.Pages.LogInPage"
             Title="Login"
             BackgroundColor="White">

    <VerticalStackLayout Spacing="20" Padding="10,40,10,10">
        <Entry TextColor ="Black" Placeholder="Email" Text="{Binding Email}" Keyboard="Email"/>
        <Entry TextColor ="Black" Placeholder="Password" Text="{Binding Password}" IsPassword="True"/>

        <Button Text="Login" Command="{Binding LoginCommand}"/>
        <Button Text="Create Account" Clicked="OnCreateAccountClicked"/>
    </VerticalStackLayout>
</ContentPage>